/repo
  index.html, style.css, dashboard.js   → committed, public
  data/pipeline.json                     → committed, public (aggregated only)
  data/raw/                              → gitignored, never committed
  scripts/build_data.py                  → committed, public
  .github/workflows/deploy.yml           → committed, public
